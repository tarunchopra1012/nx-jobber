generator client {
  provider = "prisma-client"
  output   = "../../../node_modules/@prisma-clients/jobber-auth"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id       @default(autoincrement())
  email     String   @unique
  password  String
}
